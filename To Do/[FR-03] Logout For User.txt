
As any logged-in user, I want to log out of the system so that my session ends securely.

*User clicks “Logout” button from anywhere in the application

*System destroys the session and invalidates the authentication token

*User is redirected to Login page

The user can log out to end their session safely. The system properly terminates the session upon logout.

